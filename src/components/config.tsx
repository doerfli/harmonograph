import { Typography } from "@mui/material";
import WaveConfig from "./wave_config";

export default function Config() {

    return (<>
        <Typography variant="h5">Configuration</Typography>
        <WaveConfig />
    </>);
}